kind: pipeline
type: docker
name: integration

steps:
  - name: build and test
    image: mcr.microsoft.com/dotnet/sdk:latest
    commands:
      - apt-get update && apt-get install -y nodejs npm
      - dotnet build
      - dotnet test
